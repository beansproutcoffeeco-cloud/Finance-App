{
  "name": "finance-tracker-smoke",
  "version": "1.0.0",
  "private": true,
  "description": "CI smoke-test harness for the Finance Tracker PWA (no app runtime deps).",
  "type": "module",
  "scripts": {
    "test:smoke": "node test/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.0"
  }
}
