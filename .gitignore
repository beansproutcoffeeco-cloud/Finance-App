# Node / CI tooling (dev-only; the app itself has no runtime deps)
node_modules/

# OS / editor cruft
.DS_Store
*.log

# Personal health details — never commit or sync (see training/README.md)
training/constraints.local.md
